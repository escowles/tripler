<p id="notice"><%= notice %></p>

<p>
  <strong>Vocab:</strong>
  <%= @predicate.vocab.prefix %>
</p>

<p>
  <strong>Name:</strong>
  <%= @predicate.name %>
</p>

<%= link_to 'Edit', edit_vocab_predicate_path(@vocab, @predicate) %> |
<%= link_to 'Back', vocab_path(@vocab) %>
