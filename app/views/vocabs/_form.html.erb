<%= form_with(model: vocab, local: true) do |form| %>
  <% if vocab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vocab.errors.count, "error") %> prohibited this vocab from being saved:</h2>

      <ul>
        <% vocab.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prefix %>
    <%= form.text_field :prefix %>
  </div>

  <div class="field">
    <%= form.label :uri %>
    <%= form.text_field :uri %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
